<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="12dp"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="12dp"
    app:cardBackgroundColor="?attr/cardBackgroundColor"
    app:strokeColor="?attr/cardFrameColor"
    app:cardElevation="0dp"
    app:strokeWidth="1.5dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <View
            android:id="@+id/viewSessionAccent"
            android:layout_width="12dp"
            android:layout_height="0dp"
            android:layout_alignTop="@id/contentWrapper"
            android:layout_alignBottom="@id/contentWrapper"
            android:background="@color/chalk_white" />

        <RelativeLayout
            android:id="@+id/contentWrapper"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@id/viewSessionAccent"
            android:padding="16dp">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:layout_alignParentStart="true"
                android:layout_toStartOf="@id/btnEditSession">

                <TextView
                    android:id="@+id/txtSessionName"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Class"
                    android:maxLines="2"
                    android:maxWidth="200dp"
                    android:textSize="18sp"
                    android:textStyle="bold"
                    android:ellipsize="end"
                    android:textColor="?attr/colorPrimary" />

                <TextView
                    android:id="@+id/txtSessionDetails"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Example"
                    android:textColor="?attr/descriptiveTextColor"
                    android:textSize="14sp" />
            </LinearLayout>

            <ImageView
                android:id="@+id/btnEditSession"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_centerVertical="true"
                android:layout_toStartOf="@id/imgSessionLockOnSessionView"
                android:layout_marginEnd="16dp"
                android:src="@drawable/ic_edit"
                android:clickable="true"
                android:focusable="true"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                app:tint="?attr/actionItemColor" />

            <ImageView
                android:id="@+id/imgSessionLockOnSessionView"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_alignParentEnd="true"
                android:layout_centerVertical="true"
                android:src="@drawable/status_lock"
                app:tint="?attr/actionItemColor" />

        </RelativeLayout>
    </RelativeLayout>
</com.google.android.material.card.MaterialCardView>